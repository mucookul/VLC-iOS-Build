on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-26
    permissions:
      contents: write

    steps:
      - run: sudo xcode-select -s /Applications/Xcode_26.3.app # https://stackoverflow.com/questions/79919483/use-of-private-header-from-outside-its-module-in-a-cocoapods-dependency-in-an
    
      - run: git clone --depth 1 --branch 4.0.0a7 https://code.videolan.org/videolan/vlc-ios.git

      - working-directory: vlc-ios
        run: pod install

      - working-directory: vlc-ios
        run: xcodebuild -workspace VLC.xcworkspace -scheme VLC-iOS -configuration Release CODE_SIGNING_ALLOWED=NO -derivedDataPath ./DerivedData

      - working-directory: vlc-ios
        run: |
          mkdir Payload
          cp -R ./DerivedData/Build/Products/Release-iphoneos/VLC-iOS.app Payload/
          zip -r VLC.ipa Payload/

      - working-directory: vlc-ios
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create vlc VLC.ipa --draft --repo $GITHUB_REPOSITORY
